language: minimal

services:
  - docker

install:
  - docker build -t money-server .

script:
  - docker run -t money-server /bin/sh -c "gulp test"

deploy:
  provider: script
  script: /bin/sh etc/travis/docker_deploy.sh
